SELECT
  schedule.game_id,
  schedule.season,
  schedule.game_type,
  schedule.week,
  schedule.gameday,
  schedule.weekday,
  schedule.gametime,
  schedule.away_team,
  -- schedule.away_score,
  schedule.home_team,
  -- schedule.home_score,
  schedule.game_location,
  -- schedule.game_result,
  -- schedule.total,
  schedule.overtime,
  schedule.old_game_id,
  schedule.gsis,
  schedule.nfl_detail_id,
  schedule.pfr,
  schedule.pff,
  schedule.espn,
  schedule.ftn,
  -- schedule.away_rest,
  -- schedule.home_rest,
  -- schedule.away_moneyline,
  -- schedule.home_moneyline,
  -- schedule.spread_line,
  -- schedule.away_spread_odds,
  -- schedule.home_spread_odds,
  -- schedule.total_line,
  -- schedule.under_odds,
  -- schedule.over_odds,
  schedule.div_game,
  schedule.roof,
  schedule.surface,
  schedule.temperature,
  schedule.wind,
  schedule.away_qb_id,
  schedule.home_qb_id,
  schedule.away_qb_name,
  schedule.home_qb_name,
  schedule.away_coach,
  schedule.home_coach,
  schedule.referee,
  schedule.stadium_id,
  schedule.stadium,
  home_team_code.franchise_id AS home_franchise_id,
  home_team_code.team_code AS home_team_code,
  away_team_code.franchise_id AS away_franchise_id,
  away_team_code.team_code AS away_team_code
FROM {{ ref('stg_games') }} AS schedule
INNER JOIN {{ ref('dim_team_code') }} AS home_team_code
  ON home_team_code.source_team_code = schedule.home_team
INNER JOIN {{ ref('dim_team_code') }} AS away_team_code
  ON away_team_code.source_team_code = schedule.away_team

